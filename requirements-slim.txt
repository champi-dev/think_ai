# Slimmed down requirements - remove unnecessary dependencies
# Core dependencies
fastapi==0.108.0
uvicorn[standard]==0.25.0
pydantic==2.5.3
python-multipart==0.0.6

# ML/AI dependencies - CPU only, no CUDA
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.1.2+cpu
transformers==4.30.2
sentence-transformers==2.2.2
tokenizers==0.13.3

# Data processing - minimal
numpy==1.24.3
scikit-learn==1.3.2

# Vector storage - only one
faiss-cpu==1.7.4

# Remove these heavy dependencies:
# - chromadb (200MB+)
# - langchain (100MB+)
# - pandas (50MB+)
# - accelerate (not needed for CPU)

# Utilities
httpx==0.25.2
aiofiles==23.2.1
python-dotenv==1.0.0

# Caching
diskcache==5.6.3

# Monitoring
psutil==5.9.6