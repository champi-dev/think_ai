# Think AI v5.0 Requirements
# Minimal dependencies for production deployment

# Core web framework
fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.5.0

# CORS support (included in fastapi)
# No pre-computation libraries needed!
# No heavy ML dependencies!

# That's it! True O(1) AI with minimal deps