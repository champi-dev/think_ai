# Dead simple Dockerfile for Railway debugging
FROM python:3.11-slim

WORKDIR /app

# Copy simple server (no dependencies needed!)
COPY simple_server.py .

# Railway sets PORT env var
EXPOSE 8080

# Run server
CMD ["python", "simple_server.py"]
