# Dead simple Dockerfile for Railway debugging
FROM python:3.11-slim

WORKDIR /app

# Copy files
COPY simple_server.py start.sh ./

# Make executable
RUN chmod +x start.sh simple_server.py

# Railway sets PORT env var
EXPOSE 8080

# Use start script
CMD ["./start.sh"]
