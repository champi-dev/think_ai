[phases.setup]
nixPkgs = ['rust', 'pkg-config', 'openssl']

[phases.build]
cmds = [
  'cargo build --release --bin full-server --jobs 2',
]

[phases.start]
cmd = './target/release/full-server'

[variables]
RUST_LOG = 'info'
PORT = '8080'
CARGO_BUILD_JOBS = '2'