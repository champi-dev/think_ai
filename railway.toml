[build]
builder = "nixpacks"

[deploy]
buildCommand = "cd simple-server && cargo build --release"
startCommand = "./simple-server/target/release/simple-server"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
