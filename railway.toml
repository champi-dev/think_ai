[build]
builder = "nixpacks"

[deploy]
startCommand = "./think-ai server"
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "always"

[env]
RUST_LOG = "info"
RUST_BACKTRACE = "1"