[build]
builder = "nixpacks"

[deploy]
startCommand = "./think-ai-process-manager"
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "always"

[env]
RUST_LOG = "info"
RUST_BACKTRACE = "1"