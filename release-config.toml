[profile.release]
opt-level = 3
lto = true
codegen-units = 1
strip = true

# Exclude problematic modules from release
[workspace]
exclude = ["think-ai-webapp", "think-ai-demos", "think-ai-local-llm"]
