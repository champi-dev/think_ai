# Railway Minimal Requirements - O(1) Speed Optimized
# Only essential dependencies for fastest build

# Core API
fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.5.0

# ML essentials (CPU-only for speed)
torch==2.1.0
numpy<2.0.0
transformers==4.36.0
sentence-transformers==2.2.2

# Required by app
python-multipart==0.0.6
httpx==0.25.2

# Minimal additional deps
aiofiles==23.2.1
python-jose[cryptography]==3.3.0